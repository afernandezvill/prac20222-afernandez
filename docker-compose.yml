version: '3.9'
services:
  mysql:
    image: mysql:5.7
    container_name: mysqlcontainer
    volumes:
      - './.mysql_data/db:/var/lib/mysql'
    restart: always
    ports:
      - '3306:3306'
    env_file: pass.env
  web:
    build: ./web
    container_name: web
    ports:
      - '8000:5000'
    depends_on:
      - mysql
    env_file: pass.env
      
       
   
    
       
 
    
    

    
  

 
  

